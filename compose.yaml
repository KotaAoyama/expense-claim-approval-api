services:
  postgres:
    image: postgres:16
    container_name: expense-claim-approval-postgres
    environment:
      POSTGRES_DB: expense_claim_approval
      POSTGRES_USER: app_user
      POSTGRES_PASSWORD: app_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: